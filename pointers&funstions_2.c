#include <stdio.h>

void calculateSalary(int wage, int allowance)
{

    int totalSalary = wage + allowance;
    return totalSalary;
}

int main()
{

    int totalSalary = (48, 96);
    printf("Total salary is %d", totalSalary);

    return (0);
}